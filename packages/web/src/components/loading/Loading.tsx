
const Loading = () => {
    return (
        <main className="w-screen h-screen relative overflow-y-visible">
                <span>Loading...</span>
        </main>
    );
};

export default Loading;
